# 🏗️ Архитектура Shark Spread Tracker Frontend

## 📁 Структура проекта

```
src/
├── api/                          # API слой (изолирован)
│   ├── client.ts                 # Axios instance + interceptors
│   ├── endpoints/                # Эндпоинты API
│   │   ├── tokens.api.ts
│   │   └── spreads.api.ts
│   ├── hooks/                    # React Query hooks
│   │   ├── useTokens.ts
│   │   └── useSpreads.ts
│   ├── schemas/                  # Zod схемы для валидации
│   │   ├── tokens.schema.ts
│   │   └── spreads.schema.ts
│   └── types.ts                  # API-specific типы (если нужны)
│
├── components/                    # Переиспользуемые компоненты
│   ├── ui/                       # Базовые UI компоненты (Button, Card, etc.)
│   │   ├── Button/
│   │   │   ├── Button.tsx
│   │   │   ├── Button.test.tsx
│   │   │   └── index.ts
│   │   ├── Card/
│   │   ├── Input/
│   │   ├── Select/
│   │   ├── Skeleton/
│   │   └── ErrorBoundary/
│   │
│   ├── layout/                   # Layout компоненты
│   │   ├── Header/
│   │   ├── Sidebar/
│   │   └── Container/
│   │
│   ├── features/                 # Feature-specific компоненты
│   │   ├── tokens/
│   │   │   ├── TokenSelector/
│   │   │   ├── TokenList/
│   │   │   └── TokenCard/
│   │   │
│   │   ├── spreads/
│   │   │   ├── SpreadChart/
│   │   │   ├── SpreadStats/
│   │   │   ├── SourceSelector/
│   │   │   └── TimeframeSelector/
│   │   │
│   │   └── shared/               # Shared feature components
│   │       ├── PriceDisplay/
│   │       └── SpreadIndicator/
│   │
│   └── providers/                # Context providers
│       ├── QueryProvider.tsx
│       └── ThemeProvider.tsx
│
├── constants/                     # Константы и конфигурация
│   ├── sources.ts                # Конфиг источников (MEXC, Jupiter, etc.)
│   ├── timeframes.ts             # Конфиг таймфреймов
│   ├── chains.ts                 # Конфиг блокчейнов
│   └── api.ts                    # API константы (URLs, timeouts)
│
├── hooks/                         # Переиспользуемые custom hooks
│   ├── useDebounce.ts
│   ├── useLocalStorage.ts
│   ├── useApiKey.ts              # Безопасное хранение API ключа
│   └── useSpreadCalculation.ts   # Логика расчета спреда
│
├── utils/                         # Утилиты
│   ├── format.ts                 # Форматирование (цены, даты)
│   ├── validation.ts             # Валидация
│   ├── calculations.ts           # Математические функции
│   └── errors.ts                 # Обработка ошибок
│
├── lib/                           # Внешние библиотеки конфигурация
│   └── react-query.ts            # React Query конфиг
│
├── styles/                        # Глобальные стили
│   ├── globals.css
│   └── tailwind.css
│
├── App.tsx                        # Главный компонент
├── main.tsx                       # Entry point
└── vite-env.d.ts                  # Vite типы
```

## 🎯 Принципы архитектуры

### 1. **Feature-Based Organization**
- Компоненты группируются по фичам (tokens, spreads)
- Легко найти и изменить код конкретной фичи
- Можно вынести фичу в отдельный модуль/пакет

### 2. **Component Composition**
- Мелкие переиспользуемые компоненты
- Композиция вместо наследования
- Каждый компонент = одна ответственность

### 3. **Separation of Concerns**
- **API слой** изолирован от UI
- **Бизнес-логика** в hooks и utils
- **UI компоненты** только отображение

### 4. **Type Safety**
- TypeScript везде
- Zod для runtime валидации API
- Строгая типизация пропсов

### 5. **Scalability**
- Конфигурация вместо хардкода
- Легко добавить новый источник/таймфрейм
- Расширяемая система компонентов

## 🔧 Технические решения

### API Layer
```typescript
// api/clients/*.client.ts - Axios instances для каждого источника
//   - jupiter.client.ts (https://lite-api.jup.ag)
//   - pancake.client.ts (https://api.dexscreener.com)
//   - mexc.client.ts (https://contract.mexc.com)
// api/endpoints/*.api.ts - Функции для получения данных из источников
// api/hooks/*.ts - React Query обертки
// api/schemas/*.ts - Zod валидация ответов от каждого API
```

**Важно:** Данные получаем напрямую из источников, без промежуточного бэкенда!

### State Management
- **React Query** для server state (API данные)
- **React Context** для UI state (тема, настройки)
- **Local State** для компонентного состояния

### Styling
- **Tailwind CSS** для стилей
- **CSS Variables** для темизации
- **clsx/cn** для условных классов

### Error Handling
- **Error Boundaries** на уровне роутов
- **Типизированные ошибки** (ApiError, ValidationError)
- **User-friendly сообщения**

## 📦 Переиспользуемые компоненты

### UI Components (Atomic Design)
```
ui/
├── Button        # Кнопка с вариантами (primary, secondary, ghost)
├── Card          # Карточка с header/body/footer
├── Input         # Input с валидацией
├── Select        # Select с поиском
├── Skeleton      # Skeleton loader
├── Badge         # Бейдж для статусов
├── Tooltip       # Подсказки
└── ErrorBoundary # Обработка ошибок
```

### Feature Components
```
features/
├── tokens/
│   ├── TokenSelector    # Выбор токена с поиском
│   └── TokenCard        # Карточка токена
│
└── spreads/
    ├── SpreadChart      # График спреда
    ├── SpreadStats      # Статистика спреда
    ├── SourceSelector   # Выбор источников
    └── TimeframeSelector # Выбор таймфрейма
```

## 🔐 Безопасность API ключа

**Варианты:**
1. **localStorage** (для MVP) - с шифрованием
2. **Session Storage** - очищается при закрытии
3. **HTTP-only Cookie** (лучше, но нужен бэкенд)

**Реализация:**
```typescript
// hooks/useApiKey.ts
// - Безопасное хранение
// - Валидация формата
// - Автоматическая очистка при истечении
```

## 🎨 Дизайн система

### Цветовая палитра (Dark Theme)
```typescript
// Tailwind config
colors: {
  dark: {
    50: '#f9fafb',
    100: '#f3f4f6',
    // ... до 900
  },
  primary: {
    // Основной цвет
  },
  sources: {
    mexc: '#fbbf24',
    jupiter: '#a78bfa',
    pancakeswap: '#facc15',
  }
}
```

### Компоненты дизайн-системы
- Единый стиль кнопок, карточек, инпутов
- Консистентные отступы и размеры
- Анимации и transitions

## 🚀 План реализации

### Phase 1: Foundation
1. Настройка Vite + React + TypeScript
2. Настройка Tailwind + темная тема
3. Базовая структура папок
4. API client + React Query setup

### Phase 2: Core Components
1. UI компоненты (Button, Card, Input, etc.)
2. Layout компоненты
3. Error Boundary
4. Loading states (Skeleton)

### Phase 3: Features
1. Token selector
2. Spread chart
3. Spread stats
4. Source selector
5. Timeframe selector

### Phase 4: Polish
1. Анимации
2. Оптимизация производительности
3. Тесты
4. Документация

## ✅ Checklist архитектуры

- [x] Feature-based структура
- [x] Переиспользуемые UI компоненты
- [x] Изолированный API слой
- [x] Type safety (TypeScript + Zod)
- [x] Безопасное хранение API ключа
- [x] Error handling
- [x] Loading states
- [x] Конфигурация вместо хардкода
- [x] Масштабируемость
- [x] Документированная структура

