# 🔑 Настройка API ключей

## Обзор

Shark Spread Tracker использует внешние API для получения данных о токенах и ценах. Некоторые API требуют ключи для работы, другие могут работать без ключей, но с ограничениями по rate limits.

## Требуемые API ключи

### 1. Jupiter API Key (опционально)

**Переменная окружения:** `VITE_JUPITER_API_KEY`

**Описание:** 
- Jupiter API может работать без ключа, но с ограничениями по количеству запросов
- С ключом доступны более высокие лимиты запросов
- Получить ключ можно на [Jupiter API Documentation](https://docs.jup.ag/)

**Минимальная длина:** 10 символов

### 2. MEXC API Key (опционально)

**Переменная окружения:** `VITE_MEXC_API_KEY`

**Описание:**
- MEXC API требует ключ для некоторых операций (например, получение лимитов на покупку)
- Без ключа некоторые функции могут быть недоступны
- Получить ключ можно на [MEXC API Documentation](https://mexcdevelop.github.io/apidocs/spot_v3_en/)

**Минимальная длина:** 10 символов

## Настройка

### 1. Создайте файл `.env` в корне проекта

```bash
# .env
VITE_JUPITER_API_KEY=your-jupiter-api-key-here
VITE_MEXC_API_KEY=your-mexc-api-key-here
```

### 2. Добавьте `.env` в `.gitignore`

Убедитесь, что файл `.env` не попадает в репозиторий:

```gitignore
# .gitignore
.env
.env.local
.env.*.local
```

### 3. Перезапустите dev сервер

После добавления ключей перезапустите dev сервер:

```bash
npm run dev
```

## Проверка статуса API ключей

При запуске приложения в dev режиме статус API ключей логируется в консоль:

- ✅ Все ключи валидны
- ⚠️ Отсутствуют или невалидные ключи (показывается предупреждение в UI)

## Предупреждения в UI

Если API ключи отсутствуют или невалидны, в правом верхнем углу появится предупреждение с информацией о проблеме.

Вы можете закрыть предупреждение, и оно не будет показываться до следующего запуска приложения (или до очистки localStorage).

## Влияние на функциональность

### Без API ключей:
- ✅ Базовые функции работают
- ⚠️ Могут быть ограничения по rate limits
- ⚠️ Некоторые функции могут быть недоступны (например, лимиты MEXC)

### С API ключами:
- ✅ Полный доступ ко всем функциям
- ✅ Высокие лимиты запросов
- ✅ Доступ к дополнительным данным

## Безопасность

⚠️ **ВАЖНО:** Никогда не коммитьте файл `.env` в репозиторий!

API ключи должны храниться только локально или в безопасных переменных окружения CI/CD.

## Примеры использования

### Получение ключей

1. **Jupiter API:**
   - Перейдите на [Jupiter API Documentation](https://docs.jup.ag/)
   - Зарегистрируйтесь и получите API ключ

2. **MEXC API:**
   - Перейдите на [MEXC API Documentation](https://mexcdevelop.github.io/apidocs/spot_v3_en/)
   - Создайте API ключ в личном кабинете

### Проверка валидности

Приложение автоматически проверяет валидность ключей при запуске. Невалидные ключи (короче 10 символов) будут помечены как невалидные.

## Troubleshooting

### Предупреждение не исчезает

1. Проверьте, что ключи добавлены в `.env`
2. Перезапустите dev сервер
3. Очистите localStorage: `localStorage.removeItem('api-keys-warning-dismissed')`

### Ключи не работают

1. Проверьте правильность ключей
2. Убедитесь, что ключи не содержат лишних пробелов
3. Проверьте, что ключи активны на соответствующих платформах

### Ошибки API

Если возникают ошибки API (401, 403), проверьте:
- Правильность API ключей
- Права доступа ключей
- Срок действия ключей

## Дополнительная информация

Для получения дополнительной информации см.:
- [Jupiter API Documentation](https://docs.jup.ag/)
- [MEXC API Documentation](https://mexcdevelop.github.io/apidocs/spot_v3_en/)
