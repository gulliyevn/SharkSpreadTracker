# 🚀 Инструкция по деплою на Vercel

## Быстрый старт

### 1. Подготовка проекта

Убедитесь, что проект готов к деплою:
```bash
npm run build  # Проверка сборки
npm run lint   # Проверка линтера
npm test:run   # Проверка тестов
```

### 2. Подключение к Vercel

#### Вариант A: Через Vercel Dashboard (рекомендуется)

1. Перейдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите "Add New Project"
3. Выберите ваш репозиторий `SharkFront`
4. Vercel автоматически определит настройки из `vercel.json`:
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
5. Нажмите "Deploy"

#### Вариант B: Через Vercel CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой (первый раз - интерактивно)
vercel

# Production деплой
vercel --prod
```

### 3. Настройка GitHub Secrets для автоматического деплоя

Для автоматического деплоя через GitHub Actions нужно настроить секреты:

1. Перейдите в ваш GitHub репозиторий
2. Settings → Secrets and variables → Actions
3. Добавьте следующие секреты:

#### VERCEL_TOKEN
- Получить: [Vercel Settings → Tokens](https://vercel.com/account/tokens)
- Создайте новый токен с именем `github-actions`
- Скопируйте токен и добавьте как `VERCEL_TOKEN`

#### VERCEL_ORG_ID
- Получить: [Vercel Dashboard → Settings → General](https://vercel.com/account/general)
- Найдите "Team ID" или "Organization ID"
- Добавьте как `VERCEL_ORG_ID`

#### VERCEL_PROJECT_ID
- После первого деплоя проекта в Vercel:
  - Перейдите в Settings проекта
  - Найдите "Project ID" в разделе General
  - Добавьте как `VERCEL_PROJECT_ID`

### 4. Настройка домена (опционально)

1. В Vercel Dashboard выберите ваш проект
2. Settings → Domains
3. Добавьте ваш домен
4. Следуйте инструкциям по настройке DNS

### 5. Переменные окружения (если нужны)

Если у вас есть переменные окружения:

1. Vercel Dashboard → Project → Settings → Environment Variables
2. Добавьте переменные для:
   - Production
   - Preview
   - Development

Пример:
```
VITE_API_URL=https://api.example.com
VITE_ENABLE_ANALYTICS=true
```

## Автоматический деплой

После настройки секретов, автоматический деплой работает так:

### При push в `main`:
- ✅ Запускается CI pipeline (тесты, линтер, сборка)
- ✅ Если CI успешен → автоматический деплой на production

### При создании Pull Request:
- ✅ Запускается CI pipeline
- ✅ Создается preview деплой (автоматически через Vercel)

### Ручной деплой:
```bash
# Через GitHub Actions
# Перейдите в Actions → Deploy to Vercel → Run workflow

# Или через CLI
vercel --prod
```

## Проверка деплоя

После деплоя проверьте:

1. ✅ Приложение доступно по URL от Vercel
2. ✅ Все страницы работают
3. ✅ API запросы проходят
4. ✅ Темная/светлая тема работает
5. ✅ Переключение языков работает

## Troubleshooting

### Ошибка сборки
```bash
# Проверьте локально
npm run build

# Проверьте логи в Vercel Dashboard → Deployments
```

### Ошибка деплоя через GitHub Actions
- Проверьте, что все секреты настроены правильно
- Проверьте логи в GitHub Actions

### Проблемы с роутингом (404 на refresh)
- Убедитесь, что `vercel.json` содержит правильные rewrites
- Проверьте, что `outputDirectory` указан как `dist`

## Мониторинг

### Vercel Analytics (опционально)
1. Vercel Dashboard → Project → Analytics
2. Включите Web Analytics

### Error Tracking (опционально)
Можно добавить Sentry:
1. Установите `@sentry/react`
2. Настройте в `src/main.tsx`
3. Добавьте `SENTRY_DSN` в Environment Variables

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI Reference](https://vercel.com/docs/cli)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

