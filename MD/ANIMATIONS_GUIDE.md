# 🎨 Руководство по анимациям и transitions

## Обзор

Этот документ описывает стандарты для анимаций и transitions в Shark Spread Tracker.

---

## 📋 Стандарты

### Timing Functions

Используются единые timing functions для всех анимаций:

```css
/* CSS переменные (уже определены в variables.css) */
--transition-fast: 150ms ease-in-out;
--transition-base: 200ms ease-in-out;
--transition-slow: 300ms ease-in-out;
```

**Tailwind классы:**
- `transition-colors` - для изменения цветов (150ms)
- `transition-all` - для всех свойств (200ms)
- `duration-150` - быстрые анимации
- `duration-200` - стандартные анимации
- `duration-300` - медленные анимации

### Стандартные transitions

**Кнопки:**
```tsx
className="transition-colors hover:bg-primary-600"
```

**Модальные окна:**
```tsx
className="animate-in fade-in-0 zoom-in-95 duration-200"
```

**Toast уведомления:**
```tsx
className="animate-in slide-in-from-right-full fade-in-0 duration-300"
```

**Tooltips:**
```tsx
className="animate-in fade-in-0 zoom-in-95 duration-150"
```

---

## ♿ Доступность

### prefers-reduced-motion

Все анимации автоматически отключаются для пользователей с `prefers-reduced-motion: reduce`:

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

**Проверка в коде:**
```typescript
const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

if (!prefersReducedMotion) {
  // Показываем анимацию
}
```

---

## ⚡ Оптимизация производительности

### will-change

Используйте `will-change` только для элементов, которые будут анимироваться:

```tsx
// ✅ Правильно - для элементов, которые будут анимироваться
<div className="will-change-transform">
  <AnimatedComponent />
</div>

// ❌ Неправильно - для статических элементов
<div className="will-change-transform">
  <StaticComponent />
</div>
```

**Рекомендации:**
- Используйте `will-change` только когда анимация активна
- Удаляйте `will-change` после завершения анимации
- Не используйте для большого количества элементов

### CSS Transitions vs JavaScript Animations

**Используйте CSS transitions:**
- ✅ Для простых анимаций (цвет, размер, opacity)
- ✅ Для hover эффектов
- ✅ Для появления/исчезновения элементов

**Используйте JavaScript анимации (только если необходимо):**
- ❌ Для сложных анимаций с логикой
- ❌ Для анимаций, зависящих от данных
- ❌ Для анимаций с взаимодействием

---

## 📚 Примеры

### Кнопка с hover эффектом

```tsx
<button
  className="px-4 py-2 bg-primary-600 text-white rounded-lg transition-colors hover:bg-primary-700 active:bg-primary-800"
>
  Click me
</button>
```

### Модальное окно

```tsx
<div
  className={cn(
    'fixed inset-0 z-50 flex items-center justify-center',
    'animate-in fade-in-0 duration-200'
  )}
>
  <div
    className={cn(
      'bg-white dark:bg-dark-800 rounded-lg shadow-xl',
      'animate-in zoom-in-95 duration-200'
    )}
  >
    {/* Content */}
  </div>
</div>
```

### Toast уведомление

```tsx
<div
  className={cn(
    'p-4 rounded-lg shadow-lg',
    'animate-in slide-in-from-right-full fade-in-0 duration-300'
  )}
>
  Message
</div>
```

### Skeleton loader

```tsx
<div className="animate-pulse bg-light-200 dark:bg-dark-700 rounded h-4" />
```

---

## ✅ Чеклист для разработчиков

При добавлении анимации:

- [ ] Используется CSS transition вместо JavaScript
- [ ] Используется стандартный timing function
- [ ] Добавлена поддержка `prefers-reduced-motion`
- [ ] Используется `will-change` только при необходимости
- [ ] Анимация не блокирует основной поток
- [ ] Анимация имеет разумную длительность (< 500ms)
- [ ] Анимация улучшает UX, а не отвлекает

---

## 🎯 Рекомендации

### Длительность анимаций

- **Быстрые (150ms):** Hover эффекты, tooltips
- **Стандартные (200ms):** Модальные окна, dropdowns
- **Медленные (300ms):** Toast уведомления, page transitions

### Easing functions

- **ease-in-out:** Стандартный (большинство случаев)
- **ease-out:** Для появления элементов
- **ease-in:** Для исчезновения элементов

### Когда использовать анимации

- ✅ Для обратной связи на действия пользователя
- ✅ Для плавных переходов между состояниями
- ✅ Для улучшения восприятия изменений

### Когда НЕ использовать анимации

- ❌ Для критических действий (сохранение, удаление)
- ❌ Для большого количества элементов одновременно
- ❌ Для элементов, которые должны быть мгновенными

---

## 📊 Текущее состояние

**Реализовано:**
- ✅ CSS transitions для всех интерактивных элементов
- ✅ Единые timing functions
- ✅ Поддержка `prefers-reduced-motion`
- ✅ Оптимизация через `will-change` (где необходимо)
- ✅ Tailwind анимации (animate-in, fade-in, zoom-in)

**Используемые анимации:**
- Modal: fade-in + zoom-in (200ms)
- Toast: slide-in-from-right + fade-in (300ms)
- Tooltip: fade-in + zoom-in (150ms)
- Skeleton: pulse (бесконечная)
- LoadingSpinner: spin (бесконечная)

---

**Последнее обновление:** 2024-12-20

