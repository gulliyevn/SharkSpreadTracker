# 🤝 Contributing to Shark Spread Tracker

Спасибо за интерес к проекту! Мы рады любому вкладу.

## 📋 Содержание

- [Код стиля](#код-стиля)
- [Процесс разработки](#процесс-разработки)
- [Тестирование](#тестирование)
- [Архитектура](#архитектура)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)

## 🎨 Код стиля

### TypeScript

- Используйте **TypeScript** для всех новых файлов
- Избегайте `any` - используйте конкретные типы или `unknown`
- Используйте интерфейсы для объектов, типы для union/intersection
- Добавляйте JSDoc комментарии для публичных API

```typescript
/**
 * Компонент для отображения цены токена
 * @param value - Цена токена в USD
 * @param currency - Символ валюты (по умолчанию USD)
 */
export function PriceDisplay({ value, currency = 'USD' }: PriceDisplayProps) {
  // ...
}
```

### React

- Используйте функциональные компоненты с хуками
- Используйте `React.memo` для оптимизации компонентов, которые часто ререндерятся
- Используйте `useCallback` и `useMemo` для тяжелых вычислений
- Используйте `React.lazy` для ленивой загрузки страниц

```typescript
// ✅ Хорошо
export const TokenCard = memo(function TokenCard({ token }: TokenCardProps) {
  const handleClick = useCallback(() => {
    // ...
  }, []);
  
  return <div onClick={handleClick}>...</div>;
});

// ❌ Плохо
export function TokenCard({ token }: TokenCardProps) {
  return <div onClick={() => { /* ... */ }}>...</div>;
}
```

### Стилизация

- Используйте **Tailwind CSS** для всех стилей
- Используйте утилиту `cn()` для условных классов
- Следуйте дизайн-системе проекта (цвета, отступы, размеры)

```typescript
import { cn } from '@/utils/cn';

<div className={cn(
  'base-classes',
  condition && 'conditional-classes',
  className
)} />
```

### Именование

- **Компоненты**: PascalCase (`TokenCard.tsx`)
- **Функции/переменные**: camelCase (`handleClick`, `tokenData`)
- **Константы**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Типы/интерфейсы**: PascalCase (`TokenCardProps`)
- **Файлы**: PascalCase для компонентов, camelCase для утилит

### Структура файлов

```
src/
├── components/
│   ├── ui/              # Базовые UI компоненты
│   ├── layout/          # Layout компоненты
│   └── features/        # Feature-специфичные компоненты
│       ├── tokens/
│       └── spreads/
├── pages/               # Страницы приложения
├── hooks/               # Custom hooks
├── utils/               # Утилиты
├── contexts/            # React Contexts
├── api/                 # API слой
└── types/               # TypeScript типы
```

## 🚀 Процесс разработки

### 1. Подготовка окружения

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd SharkFront

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

### 2. Создание ветки

```bash
# Создайте feature ветку
git checkout -b feature/your-feature-name

# Или bugfix ветку
git checkout -b fix/bug-description
```

### 3. Разработка

- Следуйте архитектуре проекта (см. [ARCHITECTURE.md](./MD/ARCHITECTURE.md))
- Пишите чистый, читаемый код
- Добавляйте тесты для новых компонентов/функций
- Обновляйте документацию при необходимости

### 4. Проверка перед коммитом

```bash
# Проверка TypeScript
npm run type-check

# Проверка линтера
npm run lint

# Проверка форматирования
npm run format:check

# Запуск тестов
npm test

# Исправление форматирования (если нужно)
npm run format
```

## 🧪 Тестирование

### Типы тестов

1. **Unit тесты** - для отдельных функций/компонентов
2. **Integration тесты** - для взаимодействия компонентов
3. **Smoke тесты** - для критических путей приложения

### Написание тестов

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { TokenCard } from './TokenCard';

describe('TokenCard', () => {
  it('should render token symbol', () => {
    render(<TokenCard token={{ symbol: 'BTC', chain: 'solana' }} />);
    expect(screen.getByText('BTC')).toBeInTheDocument();
  });
});
```

### Запуск тестов

```bash
# Все тесты
npm test

# Только unit тесты
npm run test:unit

# Только smoke тесты
npm run test:smoke

# С coverage
npm run test:coverage
```

## 🏗️ Архитектура

Проект следует **Feature-based layered architecture**:

- **API слой** - изолирован, использует Zod для валидации
- **Components** - разделены на ui/, layout/, features/
- **Hooks** - переиспользуемая логика
- **Contexts** - глобальное состояние (Theme, Language, Toast)
- **Pages** - страницы приложения

Подробнее см. [ARCHITECTURE.md](./MD/ARCHITECTURE.md)

## 📝 Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы коммитов

- `feat`: Новая функциональность
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование, отсутствующие точки с запятой и т.д.
- `refactor`: Рефакторинг кода
- `test`: Добавление/изменение тестов
- `chore`: Обновление зависимостей, конфигов и т.д.

### Примеры

```bash
feat(tokens): add TokenSelector component
fix(api): handle network errors gracefully
docs(readme): update installation instructions
refactor(components): optimize TokenCard with React.memo
test(tokens): add tests for TokenSelector
```

## 🔀 Pull Requests

### Процесс

1. **Обновите свою ветку** с main:
   ```bash
   git checkout main
   git pull origin main
   git checkout your-branch
   git rebase main
   ```

2. **Убедитесь, что все проверки проходят**:
   - ✅ TypeScript без ошибок
   - ✅ Линтер без ошибок
   - ✅ Все тесты проходят
   - ✅ Код отформатирован

3. **Создайте Pull Request**:
   - Опишите изменения
   - Укажите связанные issues (если есть)
   - Добавьте скриншоты (если UI изменения)

### Checklist для PR

- [ ] Код следует стилю проекта
- [ ] Добавлены тесты для новой функциональности
- [ ] Все тесты проходят
- [ ] Документация обновлена (если нужно)
- [ ] Коммиты следуют Conventional Commits
- [ ] Нет конфликтов с main

## 🐛 Сообщения об ошибках

При создании issue укажите:

- **Описание проблемы**
- **Шаги для воспроизведения**
- **Ожидаемое поведение**
- **Фактическое поведение**
- **Окружение** (браузер, ОС, версия Node.js)
- **Скриншоты** (если применимо)

## 💡 Предложения улучшений

Мы рады любым идеям! При создании issue с предложением:

- Опишите проблему, которую это решает
- Предложите решение
- Обсудите альтернативы (если есть)

## 📚 Дополнительные ресурсы

- [ARCHITECTURE.md](./MD/ARCHITECTURE.md) - архитектура проекта
- [TESTING.md](./MD/TESTING.md) - руководство по тестированию
- [TODO.md](./MD/TODO.md) - список задач
- [README.md](./README.md) - общая информация о проекте

## ❓ Вопросы?

Если у вас есть вопросы, создайте issue или свяжитесь с maintainers проекта.

---

**Спасибо за ваш вклад! 🎉**

