# 🦈 Shark Spread Tracker

Современное веб-приложение для отслеживания спредов между различными криптовалютными биржами (Jupiter, PancakeSwap, MEXC).

**Текущий режим:** Проект работает с моковыми данными для демонстрации функциональности.

## 🚀 Технологии

- **React 18** + **TypeScript** - современный стек разработки
- **Vite** - быстрая сборка и разработка
- **Tailwind CSS** - стилизация с темной темой
- **React Query (TanStack Query)** - управление состоянием и кэширование API
- **Zod** - валидация данных
- **i18next** - интернационализация (EN, RU, TR)
- **Vitest** - тестирование

## 📋 Требования

- Node.js >= 20
- npm >= 9

## 🛠️ Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd SharkFront

# Установить зависимости
npm install

# Настроить переменные окружения для локальной разработки
# Скопируй .env.example в .env.local
cp .env.example .env.local

# Отредактируй .env.local и укажи URL твоего бэкенда
# Например, если бэкенд доступен по домену:
# VITE_BACKEND_URL=https://sosal.space
```

## 🏃 Запуск

### Разработка (Dev версия)

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000` (порт настроен в `vite.config.ts`)

**Настройка для локальной разработки:**

1. **Создай `.env.local`** (уже в `.gitignore`, не коммитится):
   ```bash
   cp .env.example .env.local
   ```

2. **Укажи URL бэкенда** в `.env.local`:
   ```env
   # Основной HTTPS-URL бэкенда
   VITE_BACKEND_URL=https://sosal.space
   ```

3. **Опционально - WebSocket URL** (если нужен прямой WebSocket):
   ```env
   # Явный WebSocket-URL (если не указать, он соберётся из VITE_BACKEND_URL)
   # Для production обязательно wss://
   VITE_WEBSOCKET_URL=wss://sosal.space/socket/sharkStraight
   ```

4. **⚠️ ВАЖНО: Mixed Content проблема в dev-режиме**
   
   В dev-режиме (`http://localhost:3000`) браузер блокирует подключение к `wss://` (Mixed Content: HTTP → WSS).
   Бэкенд не поддерживает небезопасный `ws://` протокол (принудительно перенаправляет на HTTPS).
   
   **Решения:**
   
   - **Рекомендуется (для dev):** Используй мок-данные:
     ```env
     VITE_USE_MOCK_DATA=true
     ```
     - Получишь 4 токена (BTC, ETH, SOL, BNB) с реалистичными данными
     - Работающие графики с историческими данными
     - Полная функциональность UI для разработки
     - Подробнее: [MOCK_DATA_GUIDE.md](./MOCK_DATA_GUIDE.md)
   
   - **Альтернатива:** Настрой локальный HTTPS для dev-сервера (требует дополнительной конфигурации)

5. **Для отладки HTTP fallback** (принудительно использовать HTTP вместо WebSocket):
   ```env
   VITE_USE_HTTP_FALLBACK=true
   ```

**Как это работает:**
- В dev режиме Vite проксирует `/api/backend/*` на твой бэкенд (настроено в `vite.config.ts`)
- WebSocket соединения работают напрямую, если указан `VITE_WEBSOCKET_URL`
- На localhost автоматически используется HTTP fallback через прокси
- Все запросы логируются в консоль для отладки

### Сборка

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Запустить все тесты
npm test

# Запустить тесты один раз
npm run test:run

# Запустить тесты с покрытием
npm run test:coverage

# Запустить smoke тесты
npm run test:smoke

# Запустить integration тесты
npm run test:integration

# Запустить unit тесты
npm run test:unit
```

### Структура тестов

Проект использует **Vitest** для unit и integration тестов, и **Playwright** для E2E тестов.

#### Unit тесты
- Расположены рядом с тестируемыми файлами: `**/__tests__/**/*.test.{ts,tsx}`
- Тестируют отдельные функции, утилиты и компоненты
- Используют моки для изоляции тестируемого кода

#### Integration тесты
- Расположены в `src/test/integration.test.tsx`
- Тестируют взаимодействие между компонентами и API
- Проверяют работу React Query hooks

#### Smoke тесты
- Расположены в `src/test/smoke.test.tsx`
- Быстрые тесты для проверки базовой функциональности
- Запускаются в CI для быстрой проверки работоспособности

#### E2E тесты
- Расположены в `e2e/**/*.spec.ts`
- Используют Playwright для тестирования в реальном браузере
- Тестируют пользовательские сценарии

### Написание тестов

#### Пример unit теста

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MyComponent } from './MyComponent';

describe('MyComponent', () => {
  it('should render correctly', () => {
    render(<MyComponent />);
    expect(screen.getByText('Hello')).toBeInTheDocument();
  });
});
```

#### Пример теста хука

```tsx
import { describe, it, expect } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';
import { useMyHook } from './useMyHook';

describe('useMyHook', () => {
  it('should return data', async () => {
    const { result } = renderHook(() => useMyHook());
    
    await waitFor(() => {
      expect(result.current.data).toBeDefined();
    });
  });
});
```

### Покрытие кода

Целевое покрытие: **90%** для критических компонентов.

Запустить проверку покрытия:
```bash
npm run test:coverage
```

Отчет будет доступен в `coverage/index.html`.

## 📝 Скрипты

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка production версии
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода линтером
- `npm run lint:fix` - автоматическое исправление ошибок линтера
- `npm run format` - форматирование кода
- `npm run format:check` - проверка форматирования
- `npm run type-check` - проверка TypeScript типов
- `npm test` - запуск тестов в watch режиме
- `npm run test:run` - запуск тестов один раз
- `npm run test:coverage` - тесты с покрытием

## 🌐 Деплой на Vercel

### Автоматический деплой

Проект настроен для автоматического деплоя на Vercel через GitHub Actions.

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит настройки из `vercel.json`

2. **Настройте GitHub Secrets:**
   - `VERCEL_TOKEN` - токен Vercel (получить в Vercel Settings > Tokens)
   - `VERCEL_ORG_ID` - ID организации Vercel
   - `VERCEL_PROJECT_ID` - ID проекта Vercel

3. **Автоматический деплой:**
   - При каждом push в ветку `main` происходит автоматический деплой
   - Pull Request создают preview деплои

### Ручной деплой

```bash
# Установить Vercel CLI
npm i -g vercel

# Деплой
vercel --prod
```

## 💡 Примеры использования

### TokenCard - Карточка токена
```tsx
import { TokenCard } from '@/components/features/tokens/TokenCard';

<TokenCard
  token={{ symbol: 'BTC', chain: 'solana', address: '...' }}
  price={50000}
  directSpread={5.5}
  reverseSpread={-3.2}
  isFavorite={true}
  onFavoriteToggle={(token) => console.log('Toggle favorite', token)}
  onEdit={(token) => console.log('Edit token', token)}
/>
```

### SpreadChart - График спреда
```tsx
import { SpreadChart } from '@/components/features/spreads/SpreadChart';

<SpreadChart
  data={spreadData}
  sources={['mexc', 'jupiter']}
  timeframe="1h"
  chartType="spread"
/>
```

### ChartsLayout - Полный layout для ChartsPage
```tsx
import { ChartsLayout } from '@/components/features/spreads/ChartsLayout';

<ChartsLayout tokens={tokens} />
```

### API Hooks - React Query hooks

#### useTokens - Получение всех токенов
```tsx
import { useTokens } from '@/api/hooks/useTokens';

function MyComponent() {
  const { data: tokens, isLoading, error, refetch } = useTokens();
  
  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;
  
  return (
    <div>
      {tokens?.map(token => (
        <div key={token.token}>{token.token}</div>
      ))}
    </div>
  );
}
```

#### useSpreadData - Получение данных спреда для токена
```tsx
import { useSpreadData } from '@/api/hooks/useSpreadData';

function SpreadComponent({ token }) {
  const { data: spreadData, isLoading } = useSpreadData(
    token,
    '1h', // timeframe
    true  // enabled
  );
  
  if (isLoading) return <div>Loading spread data...</div>;
  
  return (
    <div>
      <p>Current spread: {spreadData?.current?.spread}%</p>
      <p>History points: {spreadData?.history?.length}</p>
    </div>
  );
}
```

#### useTokensWithSpreads - Токены с ценами и спредами
```tsx
import { useTokensWithSpreads } from '@/api/hooks/useTokensWithSpreads';

function TokensList() {
  const { data: tokens, isLoading, loadedCount, totalCount } = useTokensWithSpreads();
  
  return (
    <div>
      <p>Loaded: {loadedCount} / {totalCount}</p>
      {tokens.map(token => (
        <TokenCard key={token.token} token={token} />
      ))}
    </div>
  );
}
```

### Утилиты

#### Расчет спреда
```tsx
import { calculateSpread } from '@/utils/calculations';

const spread = calculateSpread(100, 105); // 5%
const isArbitrage = spread !== null && spread > 0;
```

#### Валидация токена
```tsx
import { validateTokenSymbol } from '@/utils/validation';

if (validateTokenSymbol('BTC')) {
  // Валидный символ
}
```

#### Санитизация данных
```tsx
import { sanitizeString, sanitizeUrl } from '@/utils/security';

const safeText = sanitizeString('<script>alert("xss")</script>');
const safeUrl = sanitizeUrl('https://example.com');
```

## 📁 Структура проекта

```
src/
├── api/              # API клиенты и endpoints
│   ├── hooks/       # React Query hooks
│   ├── endpoints/   # API endpoints (tokens, prices, spreads)
│   └── schemas/     # Zod схемы валидации
├── components/       # React компоненты
│   ├── ui/          # Базовые UI компоненты (Button, Card, Modal, etc.)
│   ├── layout/      # Layout компоненты (Header, etc.)
│   └── features/     # Feature компоненты
│       ├── tokens/  # Компоненты для работы с токенами
│       │   ├── TokenCard/          # Карточка токена
│       │   ├── TokenDetailsModal/  # Модальное окно с деталями
│       │   ├── ChainFilter/        # Фильтр по блокчейну
│       │   └── SortSelector/       # Сортировка токенов
│       └── spreads/ # Компоненты для графиков спреда
│           ├── SpreadChart/         # График спреда (recharts)
│           ├── ChartsLayout/        # Layout для ChartsPage
│           ├── TokenSelectorSidebar/ # Выбор токена
│           ├── TimeframeSelector/    # Выбор таймфрейма
│           ├── SourceSelector/       # Выбор источников
│           ├── ChartTypeToggle/      # Переключатель типа графика
│           ├── CurrentPricesPanel/    # Панель текущих цен
│           ├── SpreadAnalysisPanel/   # Панель анализа спреда
│           └── AutoRefreshToggle/     # Переключатель автообновления
├── contexts/        # React Contexts (Theme, Language, Toast)
├── hooks/           # Custom hooks
│   └── useSpreadCalculation.ts  # Хук для расчета спреда с кэшированием
├── lib/             # Библиотеки и конфигурация
│   ├── i18n.ts      # Настройка интернационализации
│   ├── sentry.ts    # Настройка Sentry (error tracking)
│   └── analytics.ts # Настройка аналитики
├── locales/         # Переводы i18n (en, ru, tr)
├── pages/           # Страницы приложения
│   ├── TokensPage.tsx   # Страница со списком токенов
│   └── ChartsPage.tsx   # Страница с графиками спреда
├── styles/          # Глобальные стили
├── types/           # TypeScript типы
└── utils/           # Утилиты
    ├── spreadHistory.ts  # Работа с историческими данными (localStorage)
    └── calculations.ts  # Математические вычисления (расчет спреда)
```

## 🎨 Особенности

### TokensPage - Список токенов
- 📊 Отображение токенов с ценами и спредами
- 🔍 Фильтрация по блокчейну (All/BSC/SOL)
- 📈 Сортировка по спреду, названию, цене
- ⭐ Избранные токены
- 🔗 Прямые ссылки на биржи (MEXC, Jupiter, PancakeSwap)
- 📋 Копирование адреса токена
- ✏️ Редактирование настроек токена (индивидуальный спред)

### ChartsPage - Графики спреда
- 📊 Интерактивные графики спреда с использованием Recharts
- 🎯 Выбор двух источников для сравнения (Direct/Reverse spread)
- ⏱️ Выбор таймфрейма (1h, 4h, 24h, 7d)
- 🔄 Автоматическое обновление данных
- 📈 Отображение текущих цен и анализа спреда
- 🎨 Цветовая индикация арбитражных возможностей
- 🔍 Zoom и pan для детального анализа
- 💾 Сохранение настроек в localStorage

### Общие возможности
- ✅ Темная/светлая тема
- ✅ Мультиязычность (EN, RU, TR)
- ✅ Адаптивный дизайн
- ✅ TypeScript для типобезопасности
- ✅ Валидация данных через Zod
- ✅ Кэширование API запросов
- ✅ Обработка ошибок
- ✅ Тестирование (600+ тестов)
- ✅ **ChartsPage** - интерактивные графики спреда с историческими данными
  - Выбор токена и таймфрейма
  - Сравнение источников (Jupiter, MEXC, PancakeSwap)
  - Визуализация прямого и обратного спреда
  - Zoom и pan функциональность
  - Автоматическое обновление данных

## 🔒 Безопасность

- XSS защита
- CSRF защита
- Security headers
- Валидация и санитизация данных
- Rate limiting

## 📚 Документация

- [AUDIT_REPORT.md](./AUDIT_REPORT.md) - полный аудит проекта
- [IMPROVEMENT_PLAN.md](./IMPROVEMENT_PLAN.md) - план улучшений проекта
- [DOCUMENTATION.md](./DOCUMENTATION.md) - документация проекта (может быть устаревшей)

## 📊 ChartsPage - Графики спреда

### Основные возможности

**ChartsPage** предоставляет интерактивные графики для анализа спреда между различными источниками:

- **Выбор токена**: Поиск и выбор токена из списка (Solana/BSC)
- **Таймфреймы**: 1m, 5m, 15m, 1h, 4h, 1d
- **Выбор источников**: Сравнение любых двух источников (Jupiter, MEXC, PancakeSwap)
- **Визуализация**: 
  - График прямого спреда (Direct Spread)
  - График обратного спреда (Reverse Spread)
  - Цветовая индикация (зеленый = арбитраж, красный = невыгодно)
- **Интерактивность**:
  - Zoom и pan (масштабирование и панорамирование)
  - Tooltip с детальной информацией
  - Автоматическое обновление данных
- **Исторические данные**: Автоматическое сохранение истории в localStorage

### Использование

1. Перейдите на страницу "Charts" в навигации
2. Выберите токен из списка слева
3. Выберите таймфрейм (1m, 5m, 15m, 1h, 4h, 1d)
4. Выберите два источника для сравнения (Buy from / Sell to)
5. Наблюдайте график спреда в реальном времени

### Компоненты

- `ChartsLayout` - основной layout с трехколоночной структурой
- `SpreadChart` - график спреда на основе recharts
- `TokenSelectorSidebar` - компактный селектор токенов
- `TimeframeSelector` - выбор таймфрейма
- `SourceSelector` - выбор источников для сравнения
- `CurrentPricesPanel` - панель текущих цен
- `SpreadAnalysisPanel` - анализ спреда с рекомендациями

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT

