# Настройка бэкенда для Shark Spread Tracker

Руководство по настройке и интеграции бэкенда с фронтендом.

## 📋 Содержание

- [Требования к бэкенду](#требования-к-бэкенду)
- [Настройка фронтенда](#настройка-фронтенда)
- [Режимы работы](#режимы-работы)
- [Health Check Endpoint](#health-check-endpoint)
- [API Endpoints](#api-endpoints)
- [WebSocket](#websocket)
- [Тестирование подключения](#тестирование-подключения)

---

## Требования к бэкенду

### 1. Health Check Endpoint

**Обязательно:** Бэкенд должен предоставлять `/health` endpoint для проверки доступности.

**Требования:**
- **URL:** `GET /health`
- **Ответ:** `200 OK` при доступности
- **Время ответа:** < 1 секунды
- **Формат ответа:** JSON (опционально)

**Пример ответа:**
```json
{
  "status": "ok",
  "timestamp": 1234567890
}
```

**Или просто:**
```
200 OK
```

### 2. API Endpoints

Бэкенд должен предоставлять следующие endpoints (если используется режим `backend` или `hybrid`):

#### Tokens API
- `GET /api/tokens` - получить все токены
- `GET /api/tokens/jupiter` - получить токены Jupiter
- `GET /api/tokens/pancake` - получить токены PancakeSwap
- `GET /api/tokens/mexc` - получить токены MEXC

#### Prices API
- `GET /api/prices?symbol=BTC&chain=solana` - получить цены токена
- `GET /api/prices/jupiter?symbol=BTC&address=...` - цена Jupiter
- `GET /api/prices/pancake?symbol=BNB` - цена PancakeSwap
- `GET /api/prices/mexc?symbol=BTC` - цена MEXC

#### Spreads API
- `GET /api/spreads?symbol=BTC&chain=solana&timeframe=1h` - получить спред

**Примечание:** Если бэкенд не предоставляет эти endpoints, фронтенд автоматически переключится на режим `direct` и будет использовать прямые вызовы к внешним API.

### 3. CORS

Бэкенд должен разрешать запросы с домена фронтенда:

```
Access-Control-Allow-Origin: https://your-frontend-domain.com
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Content-Type
```

### 4. WebSocket (опционально)

Для real-time обновлений бэкенд может предоставлять WebSocket endpoint:

- **URL:** `wss://api.your-backend.com/ws`
- **Протокол:** Согласно документации бэкенда
- **Fallback:** При ошибке WebSocket фронтенд автоматически переключится на HTTP polling

---

## Настройка фронтенда

### 1. Переменные окружения

Создайте файл `.env` в корне проекта:

```bash
# Режим работы API
VITE_API_MODE=backend  # или: direct, hybrid, auto

# URL бэкенда
VITE_BACKEND_URL=https://api.your-backend.com

# WebSocket URL (опционально, автоматически формируется из BACKEND_URL)
VITE_WEBSOCKET_URL=wss://api.your-backend.com/ws

# Интервал health check (в миллисекундах, по умолчанию 30000)
VITE_HEALTH_CHECK_INTERVAL=30000
```

### 2. Режимы работы

#### `direct` (по умолчанию)
Работает без бэкенда, использует прямые вызовы к внешним API.

```bash
VITE_API_MODE=direct
```

#### `backend`
Использует бэкенд с автоматическим fallback на direct при ошибках.

```bash
VITE_API_MODE=backend
VITE_BACKEND_URL=https://api.your-backend.com
```

#### `hybrid`
Бэкенд с Circuit Breaker и автоматическим fallback.

```bash
VITE_API_MODE=hybrid
VITE_BACKEND_URL=https://api.your-backend.com
```

#### `auto`
Автоматически определяет оптимальный режим.

```bash
VITE_API_MODE=auto
VITE_BACKEND_URL=https://api.your-backend.com
```

**Рекомендация:** Для production используйте `backend` или `hybrid` режим.

---

## Health Check Endpoint

### Проверка доступности

Фронтенд автоматически проверяет доступность бэкенда через `/health` endpoint:

```typescript
// Автоматически вызывается каждые 30 секунд (настраивается)
const isHealthy = await checkBackendHealth();
```

### Требования

1. **Быстрый ответ:** < 1 секунды
2. **Стабильность:** Endpoint должен быть доступен всегда
3. **Простота:** Минимум зависимостей (не проверять БД, внешние сервисы)

### Пример реализации (Node.js/Express)

```javascript
app.get('/health', (req, res) => {
  res.status(200).json({
    status: 'ok',
    timestamp: Date.now()
  });
});
```

### Пример реализации (Python/FastAPI)

```python
@app.get("/health")
async def health_check():
    return {"status": "ok", "timestamp": int(time.time())}
```

---

## API Endpoints

### Формат ответов

Все endpoints должны возвращать JSON:

```typescript
// Tokens
interface Token {
  symbol: string;
  chain: 'solana' | 'bsc';
  address?: string;
}

// Prices
interface TokenPrice {
  price: number;
  timestamp: number;
  source: 'jupiter' | 'pancakeswap' | 'mexc';
}

// Spreads
interface SpreadResponse {
  current: {
    directSpread: number | null;
    reverseSpread: number | null;
  };
  sources: {
    jupiter: TokenPrice | null;
    pancakeswap: TokenPrice | null;
    mexc: TokenPrice | null;
  };
  history?: SpreadDataPoint[];
}
```

### Обработка ошибок

Бэкенд должен возвращать стандартные HTTP коды:

- `200 OK` - успешный запрос
- `400 Bad Request` - неверные параметры
- `404 Not Found` - ресурс не найден
- `429 Too Many Requests` - превышен rate limit
- `500 Internal Server Error` - ошибка сервера
- `502 Bad Gateway` - ошибка внешнего API
- `503 Service Unavailable` - сервис недоступен

При ошибках (4xx, 5xx) фронтенд автоматически переключится на режим `direct`.

---

## WebSocket

### Настройка

Если бэкенд предоставляет WebSocket:

```bash
VITE_WEBSOCKET_URL=wss://api.your-backend.com/ws
```

Или автоматически формируется из `VITE_BACKEND_URL`:
```
wss://api.your-backend.com/ws
```

### Протокол

Протокол WebSocket должен быть согласован с бэкендом. Базовая архитектура готова:

- `src/api/clients/websocket.client.ts` - WebSocket клиент
- `src/api/adapters/websocket-adapter.ts` - адаптер с fallback
- `src/api/hooks/useWebSocketData.ts` - React Query hooks

**После получения документации от бэкенда:**
1. Настроить протокол подписки
2. Настроить протокол отписки
3. Настроить обработку сообщений

### Fallback

При ошибке WebSocket фронтенд автоматически переключится на HTTP polling.

---

## Тестирование подключения

### 1. Проверка Health Check

```bash
curl https://api.your-backend.com/health
```

Ожидаемый ответ: `200 OK`

### 2. Проверка из браузера

Откройте консоль браузера и выполните:

```javascript
fetch('https://api.your-backend.com/health')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);
```

### 3. Проверка режима работы

В консоли браузера:

```javascript
// Проверить текущий режим
console.log(import.meta.env.VITE_API_MODE);

// Проверить URL бэкенда
console.log(import.meta.env.VITE_BACKEND_URL);
```

### 4. Логи в консоли

Фронтенд логирует все переключения режимов:

```
[BackendApiAdapter] Auto-switched to direct for getAllTokens
[BackendApiAdapter] Backend recovered, switching back to backend
```

---

## Миграция с direct на backend

### Шаг 1: Настройте бэкенд

1. Создайте `/health` endpoint
2. Настройте CORS
3. (Опционально) Создайте API endpoints

### Шаг 2: Обновите .env

```bash
VITE_API_MODE=backend
VITE_BACKEND_URL=https://api.your-backend.com
```

### Шаг 3: Перезапустите фронтенд

```bash
npm run dev
```

### Шаг 4: Проверьте работу

1. Откройте приложение
2. Проверьте консоль браузера на наличие ошибок
3. Проверьте, что данные загружаются
4. Проверьте компонент `BackendStatus` (если используется)

---

## Troubleshooting

### Бэкенд недоступен

**Симптомы:**
- Фронтенд переключается на `direct` режим
- В консоли: `[BackendApiAdapter] Auto-switched to direct`

**Решение:**
1. Проверьте, что `/health` endpoint работает
2. Проверьте CORS настройки
3. Проверьте URL бэкенда в `.env`

### CORS ошибки

**Симптомы:**
- В консоли: `CORS policy: No 'Access-Control-Allow-Origin' header`

**Решение:**
Настройте CORS на бэкенде для домена фронтенда.

### Медленный ответ бэкенда

**Симптомы:**
- Фронтенд переключается на `direct` при медленных ответах (> 3 секунд)

**Решение:**
1. Оптимизируйте бэкенд
2. Увеличьте таймаут (не рекомендуется)
3. Используйте кэширование на бэкенде

---

## Дополнительные ресурсы

- [API_MODES.md](./API_MODES.md) - подробная документация по режимам работы
- [API_INTEGRATION.md](./API_INTEGRATION.md) - руководство по интеграции с API
- [README.md](../README.md) - общая информация о проекте

---

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте настройки `.env`
3. Проверьте доступность `/health` endpoint
4. Проверьте CORS настройки бэкенда

