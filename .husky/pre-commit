#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Запускаем lint-staged для проверки и форматирования только измененных файлов
npx lint-staged

# Опционально: запускаем type-check (быстрая проверка типов)
# Можно раскомментировать если нужна проверка типов перед каждым коммитом
# npm run type-check
