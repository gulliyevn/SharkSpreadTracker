# SharkSpreadTracker - Environment Variables Example
# Скопируй этот файл в .env.local для локальной разработки
# cp .env.example .env.local

# URL бэкенда (обязательно для работы приложения)
VITE_BACKEND_URL=https://sosal.space

# WebSocket URL (опционально)
# Если не указан, будет сформирован из VITE_BACKEND_URL
VITE_WEBSOCKET_URL=wss://sosal.space/socket/sharkStraight

# Принудительное использование HTTP fallback вместо WebSocket
# Установите в 'true' для отладки или если WebSocket не работает
# VITE_USE_HTTP_FALLBACK=false

# Использовать мок-данные для разработки UI без бэкенда
# ВАЖНО: 
# - Dev-режим (http://localhost:3000): РЕКОМЕНДУЕТСЯ true (из-за Mixed Content)
# - Production (https://): ДОЛЖНО БЫТЬ false
VITE_USE_MOCK_DATA=false
